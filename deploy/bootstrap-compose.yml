name: rokhdad-bootstrap

services:
  web:
    image: nginx:1.27-alpine
    container_name: rokhdad-bootstrap-web
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./bootstrap/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ../public/landing:/usr/share/nginx/html:ro

